#!/bin/bash

# value="${HOME}/scripts\n${HOME}/c++\n${HOME}/tempoky"

# select=$(echo -e "$value" | dmenu )
# select=$(echo -e "$value" | rofi -dmenu )
# select=$(echo -e "$value" | fzf )

# nautilus "$select" &

